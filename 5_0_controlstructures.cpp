#include <iostream>
using namespace std;

int main()
{
    // control structures
    /*
    1. sequence structures - helps actions/code execute one after another
    2. selection structures - helps actions/code execute after a yes/no or tru/false condition (eg. if-else cond)
    3. loop structures - helps actions/block of code get executed multiple times for a while (eg. whjile, dowhile)   */

    /*
    Sequence Structure: default case where each code is executed one after other
    Selection Structure:
        If-else Statement
        Switch Statement
    Loop Structure:
        While Loop
        Do-While Loop
        For Loop
         */
    return 0;
}